[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "calc_binding_alistaire_noiprasit"
version = "0.2.0"

[project.scripts]
calc_binding_alistaire_noiprasit = "calc_binding_alistaire_noiprasit.__main__:main"